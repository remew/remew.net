import {ThemeScheme} from './type';

import backgroundImageUrl from '../assets/images/background-light.jpg';

const theme: ThemeScheme = {
  backgroundImageUrl,

  contentBackgroundColor: '#fff',

  footerBackgroundColor: '#eee',
  footerTextColor: '#212121',

  iconBackgroundColor: '#fff',
};

export default theme;
